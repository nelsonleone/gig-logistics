import { roboto_slab } from "../fonts";

export default function GIGLBlog(){
    return(
        <div className="page w-full  flex justify-center items-center">
            <h1 className={`${roboto_slab.className} text-center text-4xl font-bold text-black`}>Blog</h1>
        </div>
    )
}